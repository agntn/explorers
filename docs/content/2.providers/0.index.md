---
title: Providers
icon: i-lucide-library
navigation:
  title: Overview
description: Fifteen provider classes behind one contract. Pick one to see its auth and its endpoints and what it maps. And the traps.
---

Every provider answers the same nine questions with the same shapes. What changes underneath is the API, where the key travels, how a page says there's a next one, and which operations exist at all. The pages are short on purpose. The part worth reading is the gotcha list on each one, because that's where the afternoons go.

::provider-matrix
::

## Four families

**EVM, ten chains each.** [Etherscan](/providers/etherscan) and [Blockscout](/providers/blockscout) each serve ten EVM chains and every operation. Etherscan needs a key and answers in one v2 endpoint with a chain id; Blockscout needs nothing and has one host per chain. With a key you get Etherscan first, without one Blockscout, and either way a rate limit on the first moves the read to the other.

**Bitcoin and its relatives.** [Mempool](/providers/mempool) for Bitcoin, Litecoin and Pepecoin through mempool.space and its siblings, [Blockstream](/providers/blockstream) for Bitcoin through the Esplora API as an independent second backend, [Blockchair](/providers/blockchair) for Bitcoin, Bitcoin Cash, Ethereum and eCash through one dashboard API, and [dcrdata](/providers/dcrdata) for Decred through Insight. The first two are the only ones with unspent outputs.

**One chain, with a key.** [Solscan](/providers/solscan) and [Helius](/providers/helius) on Solana, [TRONSCAN](/providers/tronscan) on TRON, [Blockberry](/providers/blockberry) on Sui. Each throws `UnsupportedChainError` for any other chain and `AuthError` without its key.

**One chain, no key.** [TONAPI](/providers/ton), [Koios](/providers/koios) on Cardano, the [Arweave gateway](/providers/arweave) and [Horizon](/providers/horizon) on Stellar. And [Aptos](/providers/aptos), which is registered, honest and empty.

## Adding one

A new backend is one class, one key, one entry in `builtins` and one line in the build config. [Custom providers](/guide/custom) walks through it; the registry test refuses a provider that skips a step.
